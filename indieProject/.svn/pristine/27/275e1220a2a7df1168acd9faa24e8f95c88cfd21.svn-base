/**
 * 고객센터
 */
function submit(){
	var memberId = $("input[name='memberId']").val();
	var memberEmail = $("input[name='memberEmail']").val();
	var message = $("#message").val();
	//카테고리 값 가져오기
	var category=$("select[name='category']").val();
	//var form=document.customerForm;
	
	
	if(memberId==""){
		alert("아이디를 입력해주세요.");
		return false;
	}else if(email==""){
		alert("이메일을 입력해주세요.");
		return false;
	}else if(category==""){
		alert("문의내용을 선택해주세요");
		return false;
	}else if(message==""){
		alert("문의내용을 입력해주세요.");
		return false;
	}else {
		$.ajax({
		url : contextPath+"/member/MemberCustomerAction.me",
		type:"post",
		data:({memberId :memberId, memberEmail : memberEmail,message : message,category : category}),
		datatype:"text",
		success : function(result){
			if(result.trim()=="no"){
				console.log("없는 회원");
				alert("없는 회원입니다.");
			}else{
				console.log("있는 회원");
				alert("문의내역이 정상적으로 접수되었습니다.");
				//창 새로고침 해주기 
				history.go(0);
			}
		},
		error:function(){//통신 오류 시
			console.log("오류");
		}
	})
}
}